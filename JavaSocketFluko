ServerSocket = new ServerSocket(9999);
for(;;){

	//Abrir
	Socket cl = s.accept(); // Conexion acept
	ada del cliente
	ObjetOutputStream oos = new ObjetOutputStream(cl.OutputStream);
	ObjetInputStream ois = new ObjetInputStream(cl.OutputStream);

	// Enviar datos
	ObjetoX o = new ObjetoX(......);
	oos.writeObject();


	//Recibir datos
	Object o3 = ois.readObject();
	// Verificar a que encaja
	if(o3 instanceof ObjectoClass1){
		Objecto oo = (ObjectoClass1);
	}
	.
	.
	.
	elseif(){

	}

	//Cerrar

}